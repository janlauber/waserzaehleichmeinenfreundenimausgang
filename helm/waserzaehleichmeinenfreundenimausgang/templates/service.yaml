apiVersion: v1
kind: Service
metadata:
  name: {{ include "waserzaehleichmeinenfreundenimausgang.fullname" . }}
  labels:
    {{- include "waserzaehleichmeinenfreundenimausgang.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "waserzaehleichmeinenfreundenimausgang.selectorLabels" . | nindent 4 }}

